source "https://rubygems.org"

gemspec

gem "rake", ">= 10.0", "< 14.0"
gem "minitest", ">= 5.0", "< 6.0"

# Exercise optional host asset integration; not a gem runtime dependency.
gem "sprockets-rails", ">= 3.0", "< 4.0", :require => false

# Local browser fixture server; never a gem runtime dependency.
gem "webrick", "~> 1.8", :require => false
